import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const VendorProject = () => {
  return (
    <View>
      <Text>This is Vendor Project</Text>
    </View>
  );
};

export default VendorProject;

const styles = StyleSheet.create({});
